# Shared Packages

This directory contains reusable contracts, models, utilities, and libraries shared across LingoMind components.

## Rules

- Keep packages focused and independently testable.
- Avoid coupling shared code to UI frameworks or infrastructure providers.
- Security-sensitive helpers must have explicit tests and documented assumptions.
