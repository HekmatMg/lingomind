# Applications

This directory contains user-facing applications.

## Planned applications

- `client/` — Flutter client for mobile and supported desktop targets.

Application code should depend on stable domain/application contracts rather than concrete AI providers or infrastructure implementations.
